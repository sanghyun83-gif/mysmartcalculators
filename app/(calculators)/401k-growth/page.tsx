import { HubClient } from "./HubClient";

export const metadata = {
    title: "401k Growth Calculator | Retirement Wealth Audit",
    description: "Professional-grade estimator for 401k retirement growth. Analyze tax-advantaged compounding, contribution limits, and retirement scaling benchmarks.",
};

export default function FinancePage() {
    return <HubClient />;
}
